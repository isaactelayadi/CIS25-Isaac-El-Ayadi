This is my code for the week 6 assignment on module 6. The first part is declaring the class and the members. The class item and private define a new item type and make sure that the declared members are encapsulated.
There's also the string name and quantity. These just say the item's name and how many are stored.
The public means that everything can be accessed from outside codes. The user can also change an item's name without having to access private members.
Load from file reads and prints. Different infiles or check that the file successfully opened and also prints into the cout
The main function sets the data and quantity and writes a line into the text. Then the return finishes the program
