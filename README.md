Module 6,
The first things are the strings and those hold the name of the item. Then
the classes are defined by the user and bundle the data and functions. The 
streams let the user read and write text.
Module 11,
The constructor runs right as the object is created and prints that while the
The destructor will also run automatically when the item leaves the main.
Lastly, the printDetails will display all of the stored data to be verified.
